apiVersion: v1
kind: Service
metadata:
  name: comment-service
  namespace: task-api
  labels:
    app: comment-service
spec:
  selector:
    app: comment-service
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP