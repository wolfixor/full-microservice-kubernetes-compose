# Raw Operator Bundles

Offline recovery copies from the original lab installation.

These files are not reconciled by Argo CD and are not the normal production
installation path. Prefer `k8s/operators/helmfile.yaml.gotmpl` on new clusters.
