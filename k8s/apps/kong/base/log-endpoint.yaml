apiVersion: v1
kind: Service
metadata:
  name: kong-log-endpoint
  namespace: task-api
spec:
  type: ClusterIP
  selector:
    app: kong-log-receiver
  ports:
  - port: 8001
    targetPort: 8001
    name: http
