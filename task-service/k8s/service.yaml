apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-api
  labels:
    app: task-service
spec:
  selector:
    app: task-service
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP