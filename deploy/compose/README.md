# Docker Compose

Local development configuration used by the root `docker-compose.yml`.

`config/` contains mounted configuration for Kong, Prometheus, Grafana,
Kibana, Fluent Bit, and pgAdmin. It is not consumed by Kubernetes or Argo CD.
