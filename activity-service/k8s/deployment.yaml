apiVersion: apps/v1
kind: Deployment
metadata:
  name: activity-service
  namespace: task-api
  labels:
    app: activity-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: activity-service
  template:
    metadata:
      labels:
        app: activity-service
    spec:
      containers:
      - name: activity-service
        image: registry.gitlab.shiveh.com/mapir/upstream/kibana:activity-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRES_HOST
          value: "activity-service-db"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_DB
          value: "activity_db"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: POSTGRES_PASSWORD
        - name: ROOT_PATH
          value: "/activities"
        - name: KAFKA_ENABLED
          value: "true"
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "platform-kafka-kafka-bootstrap.kafka.svc:9092"
        - name: KAFKA_CONSUMER_GROUP_ID
          value: "activity-service"
        - name: KAFKA_AUTO_OFFSET_RESET
          value: "earliest"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: activity-service
  namespace: task-api
  labels:
    app: activity-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"
spec:
  selector:
    app: activity-service
  ports:
  - name: http
    port: 80
    targetPort: 8000
  type: ClusterIP
