1. the single ingest of the search service needed error handeling now it return 500
2. we have problem in the root path for example i want this work on both of the compose and kuber
 now we add hardcode base root on the main.py for each service